{
	"name": "empress",
	"displayName": "empress",
	"description": "Confine your Liberty",
	"version": "1.0.0",
	"engines": {
		"vscode": "^1.36.0"
	},
	"categories": [
		"Other"
	],
	"author": {
		"name": "Reinier van Leussen",
		"email": "info@rvlstudio.com",
		"url": "https://rvlstudio.com"
	},
	"publisher": "rvlstudio",
	"repository": {
		"url": "https://github.com/reiniervl/empress.git",
		"type": "git"
	},
	"license": "(MIT OR Apache-2.0)",
	"icon": "media/logo_square.png",
	"galleryBanner": {
		"color": "#C80000",
		"theme": "dark"
	},
	"activationEvents": [
		"onView:empressView"
	],
	"main": "./out/extension.js",
	"contributes": {
		"configuration": [
			{
				"title": "empress",
				"properties": {
					"empress.openlibertyBaseDirectory": {
						"type": [
							"string"
						],
						"description": "path to the folder where openliberty is installed ('*/wlp/')"
					}
				}
			}
		],
		"commands": [
			{
				"command": "empress.start",
				"title": "start",
				"icon": {
					"dark": "media/dark/play.svg",
					"light": "media/light/play.svg"
				}
			},
			{
				"command": "empress.stop",
				"title": "stop",
				"icon": {
					"dark": "media/dark/stop.svg",
					"light": "media/light/stop.svg"
				}
			},
			{
				"command": "empress.refresh",
				"title": "refresh"
			}
		],
		"views": {
			"explorer": [
				{
					"id": "empressView",
					"name": "empress"
				}
			]
		},
		"menus": {
			"view/title": [
				{
					"command": "empress.refresh",
					"when": "view == empressView",
					"group": "navigation"
				}
			],
			"view/item/context": [
				{
					"command": "empress.start",
					"when": "view == empressView && viewItem == notrunning",
					"group": "inline"
				},
				{
					"command": "empress.stop",
					"when": "view == empressView && viewItem == running",
					"group": "inline"
				}
			]
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"pretest": "npm run compile",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"@types/glob": "^7.1.1",
		"@types/mocha": "^5.2.6",
		"@types/node": "^10.12.21",
		"@types/vscode": "^1.36.0",
		"glob": "^7.1.4",
		"mocha": "^6.1.4",
		"typescript": "^3.3.1",
		"tslint": "^5.12.1",
		"vscode-test": "^1.0.2"
	}
}
